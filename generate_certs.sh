#!/bin/bash--- OpenEMR PoC SSL Certificate Generator ---This script creates a self-signed certificate and key for localhost.It is necessary for testing OpenEMR's OAuth and FHIR APIs via HTTPS.1. Ensure the 'certs' directory existsmkdir -p certscd certs2. Generate the self-signed certificate and keyNote: C=US, ST=NY, L=NewYork, O=PoC Test, CN=localhostopenssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout localhost.key -out localhost.crt -subj "/C=US/ST=NY/L=NewYork/O=PoC Test/CN=localhost"echo "✅ Self-signed SSL certificates generated in the 'certs' directory:"echo "   - localhost.key"echo "   - localhost.crt"echo "---"echo "You can now run 'docker-compose up -d' to start OpenEMR via HTTPS on port 8443."
